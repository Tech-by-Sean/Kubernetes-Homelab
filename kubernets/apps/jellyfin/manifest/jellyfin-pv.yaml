# jellyfin-pv.yaml
apiVersion: v1
kind: PersistentVolume
metadata:
  name: jellyfin-main-pv
spec:
  capacity:
    storage: 1100Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  hostPath:
    path: /mnt/jellyfin-data
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - talos-worker-01 # ⚠️ Replace this with the actual hostname